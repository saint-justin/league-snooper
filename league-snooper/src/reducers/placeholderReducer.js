// TODO: 
// Replace me with all the reducers needed to sort out info passed in from DDragon or League API

const placeholderReducer = (state=0, action) => {
    switch (action.type){
        default:
            return state;
    }
}

export default placeholderReducer;